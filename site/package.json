{
  "name": "lernspur-preview",
  "private": true,
  "version": "1.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5187 --strictPort",
    "build": "tsc --noEmit && vite build --base=./",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.1.5",
    "@playwright/test": "^1.55.0"
  }
}
